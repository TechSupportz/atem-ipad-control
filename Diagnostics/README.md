# Physical ATEM captures

`original-atem-mini-initial-state-20260726.log` is the complete initial state
transfer captured from the user's original ATEM Mini at `192.168.18.240` on
26 July 2026.

The capture begins with the handshake response, contains every initial state
datagram through `InCm`, and was recorded before sending any switching
commands. Preserve it as the ground truth for future protocol parsing,
especially the deferred Fairlight audio milestone.
